import { RecordsSection } from '../components/RecordsSection'

/** 记录页：请求 / 事件 / 进程树。对应原「5b4. 记录」，自包含组件，无需外部状态。 */
export function RecordsPage() {
  return (
    <div>
      <RecordsSection />
    </div>
  )
}
